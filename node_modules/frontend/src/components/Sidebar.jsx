import React from "react";
import { NavLink } from "react-router-dom";
import {
  LayoutDashboard,
  Receipt,
  BarChart3,
  LogOut,
} from "lucide-react";

export default function Sidebar() {
  const linkClasses =
    "flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200";

  return (
    <div className="h-screen w-64 bg-slate-900 text-white flex flex-col p-4 border-r border-white/10">

      {/* LOGO */}
      <h1 className="text-2xl font-bold mb-10 text-center tracking-wide">
        ExpenseX
      </h1>

      {/* NAV LINKS */}
      <nav className="flex flex-col gap-2 flex-1">

        <NavLink
          to="/dashboard"
          className={({ isActive }) =>
            `${linkClasses} ${
              isActive
                ? "bg-white/10 text-white shadow-lg"
                : "text-gray-400 hover:bg-white/5 hover:text-white"
            }`
          }
        >
          <LayoutDashboard size={20} />
          Dashboard
        </NavLink>

        <NavLink
          to="/transactions"
          className={({ isActive }) =>
            `${linkClasses} ${
              isActive
                ? "bg-white/10 text-white shadow-lg"
                : "text-gray-400 hover:bg-white/5 hover:text-white"
            }`
          }
        >
          <Receipt size={20} />
          Transactions
        </NavLink>

        <NavLink
          to="/reports"
          className={({ isActive }) =>
            `${linkClasses} ${
              isActive
                ? "bg-white/10 text-white shadow-lg"
                : "text-gray-400 hover:bg-white/5 hover:text-white"
            }`
          }
        >
          <BarChart3 size={20} />
          Reports
        </NavLink>

      </nav>

      {/* LOGOUT */}
      <button className="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:bg-red-500/20 hover:text-red-400 transition">
        <LogOut size={20} />
        Logout
      </button>
    </div>
  );
}
